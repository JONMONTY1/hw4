<h1>Places</h1>

<ul>
  <% for place in @places %>
    <% if @current_user !=nil %>
    <a href="/places/<%= place["id"] %>"><%= place["name"] %></a>
    <% end %>
  <% end %>
</ul>

<p>
  <% if @current_user !=nil %>
    <a href="/places/new">New place</a>
  <% end %>
</p>